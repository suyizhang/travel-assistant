# 🧭 旅伴 — 产品设计审查与优化建议
# 审查时间：2026-02-26
# 审查人：AI 产品设计顾问

==============================================================================
# 一、用户体验（UX）优化
==============================================================================

## 1.1 聊天页 — 流式输出（高优先级）⭐⭐⭐
- 现状：用户发送消息后，需要等待 AI 完整回复才能看到内容（loading 三个点动画）
       后端已有 /api/chat/stream SSE 接口，但前端未接入
- 问题：AI 回复通常需要 5-15 秒，用户等待体验极差，容易误以为卡死
- 建议：前端接入 SSE 流式输出，让用户实时看到 AI 逐字回复
       参考 ChatGPT 的打字机效果，大幅提升对话流畅感
- 涉及文件：packages/web/src/pages/chat/index.tsx
            packages/web/src/services/api.ts

## 1.2 聊天页 — 消息重试与错误恢复（高优先级）⭐⭐⭐
- 现状：请求失败后直接显示错误文本，用户无法重试
- 建议：
  (1) 错误消息旁添加「重试」按钮，点击可重新发送同一条消息
  (2) 网络断开时显示全局提示横幅
  (3) 考虑添加消息发送中的取消功能

## 1.3 聊天页 — 历史消息持久化（中优先级）⭐⭐
- 现状：聊天记录仅存在内存中，刷新页面或返回首页后丢失
- 问题：用户可能中途退出再回来，发现之前的旅行规划全部丢失
- 建议：
  (1) 使用 Taro.setStorage 将消息列表持久化到本地
  (2) 进入聊天页时自动恢复上次会话
  (3) 添加「新对话」按钮，让用户主动开启新会话

## 1.4 首页 — 快捷问题与季节/时间关联（中优先级）⭐⭐
- 现状：首页快捷问题是硬编码的 4 个固定问题（五一、北京三日游、成都预算、三亚天气）
- 问题：「推荐五一去哪玩？」在非五一时段显得不合时宜
- 建议：
  (1) 根据当前日期/季节动态生成快捷问题（如冬季推滑雪、夏季推海岛）
  (2) 根据即将到来的假期推荐（春节、清明、五一、国庆等）
  (3) 也可后端提供一个接口返回推荐问题

## 1.5 聊天页 — 空状态与首次引导优化（低优先级）⭐
- 现状：空状态只有 2 个快捷问题入口
- 建议：
  (1) 增加到 4-6 个快捷问题，分类展示（行程/天气/预算/景点）
  (2) 添加简短的使用说明（如"你可以问我任何旅行问题"下面列几个例子）
  (3) 展示 AI 能力卡片，让用户了解可以问什么

==============================================================================
# 二、功能完善
==============================================================================

## 2.1 评价系统 — 前端入口缺失（高优先级）⭐⭐⭐
- 现状：后端已有完整的评价 CRUD API（提交评价、查看评价、标记有用、统计等）
       但前端完全没有评价相关的 UI
- 问题：评价功能完全不可用，等于白做
- 建议：
  (1) 在 AI 推荐景点时，消息中嵌入「查看评价」按钮
  (2) 新增「景点评价」页面，展示评分、评价列表、标签云
  (3) 支持用户提交评价（评分 + 文字 + 标签选择）
  (4) 在 AI 对话中，当提到某景点时，可以展示口碑摘要卡片

## 2.2 流式聊天接口 — session 未正确传递（中优先级）⭐⭐
- 现状：/api/chat/stream 路由中 streamChat 未传入 sessionId
       streamChat(res, message, [], corsHeaders) — 第3参数是空数组
- 问题：流式聊天没有会话记忆能力，每次都是全新对话
- 建议：修复 streamChat 调用，传入正确的 sessionId 和历史消息
- 涉及文件：packages/server/src/routes.ts (第167行)
            packages/server/src/stream.ts

## 2.3 分享功能（中优先级）⭐⭐
- 现状：无任何分享能力
- 建议：
  (1) 支持将 AI 生成的旅行规划导出为图片/长截图
  (2) 微信小程序端支持分享到好友/朋友圈
  (3) H5 端支持复制分享链接

## 2.4 多轮对话中的上下文提示（低优先级）⭐
- 现状：用户不清楚 AI 记住了哪些信息
- 建议：
  (1) 在聊天顶部显示当前会话的上下文标签（如「目的地：三亚 | 天数：3天 | 预算：中等」）
  (2) 让用户可以一键修改这些参数

==============================================================================
# 三、交互与视觉
==============================================================================

## 3.1 Markdown 渲染增强（中优先级）⭐⭐
- 现状：自定义 Markdown 组件只支持：标题、加粗、斜体、行内代码、列表、分割线
- 问题：AI 回复经常包含表格（预算对比）、链接、代码块等，这些都无法正确渲染
- 建议：
  (1) 增加表格渲染支持（预算估算经常以表格形式返回）
  (2) 增加链接渲染支持（可点击跳转）
  (3) 增加编号列表样式区分（目前有序无序列表样式相同，都是 • 圆点）

## 3.2 消息气泡 — 操作菜单（低优先级）⭐
- 现状：消息气泡无任何交互操作
- 建议：
  (1) 长按/右键消息弹出菜单：复制文本、收藏
  (2) AI 消息底部添加反馈按钮：👍 有用 / 👎 不太好
  (3) 用户消息支持编辑重发

## 3.3 输入体验优化（低优先级）⭐
- 现状：只有单行 Input，输入长文本不方便
- 建议：
  (1) 改为自适应高度的 Textarea，输入多行时自动扩展
  (2) 添加语音输入按钮（小程序端可调用微信语音识别）
  (3) 发送按钮在有内容时才高亮，无内容时置灰

==============================================================================
# 四、性能与技术债
==============================================================================

## 4.1 数据层 — 静态数据硬编码问题（中优先级）⭐⭐
- 现状：天气、景点、预算、行程数据全部硬编码在 TypeScript 文件中
       仅覆盖 16-17 个城市
- 问题：
  (1) 数据无法动态更新，天气数据尤其需要实时性
  (2) 城市覆盖面太少，用户问到其他城市时 AI 无法提供工具数据
- 建议：
  (1) 短期：在 AI 提示词中明确告知未覆盖城市时的回复策略
  (2) 中期：接入真实天气 API（如和风天气/心知天气）
  (3) 长期：景点/行程数据迁移到数据库，支持管理后台编辑

## 4.2 会话管理 — 内存存储风险（中优先级）⭐⭐
- 现状：所有会话数据存储在 Node.js 进程内存的 Map 中
- 问题：
  (1) 服务重启后所有会话丢失
  (2) 多实例部署时会话不共享
  (3) 长时间运行可能内存泄漏（虽然有过期清理，但 2h TTL 内大量用户仍可能撑爆）
- 建议：
  (1) 短期：增加最大会话数限制，达到上限时 LRU 淘汰
  (2) 中期：迁移到 Redis 存储会话

## 4.3 前端 getUserInfo 重复定义（低优先级）⭐
- 现状：getUserInfo() 函数在 index.tsx 和 chat/index.tsx 中完全重复定义
- 建议：提取到公共 utils 模块

==============================================================================
# 五、安全与合规
==============================================================================

## 5.1 登录方式单一（中优先级）⭐⭐
- 现状：H5 端仅支持 GitHub OAuth，小程序端仅支持微信登录
- 问题：GitHub 登录门槛过高，普通旅行用户大多不是程序员，没有 GitHub 账号
- 建议：
  (1) H5 端增加微信扫码登录 / 手机号验证码登录
  (2) 或者提供「游客体验」模式，限制每日免费次数（如 5 次对话）

## 5.2 评价内容安全（中优先级）⭐⭐
- 现状：评价提交只做了长度校验（5-500字），无内容审核
- 问题：可能出现广告、敏感信息、不当言论
- 建议：接入内容安全审核（腾讯云天御/其他 NLP 审核服务）

==============================================================================
# 六、优先级排序（推荐实施路径）
==============================================================================

# P0 — 立即修复（体验硬伤）：
#   [1.1] 接入流式输出（后端已有，前端未对接）
#   [2.1] 评价系统前端入口（后端已有，前端缺失）
#   [2.2] 修复流式接口 session 传递 bug

# P1 — 短期优化（1-2 周）：
#   [1.2] 消息重试与错误恢复
#   [1.3] 聊天记录本地持久化
#   [3.1] Markdown 表格/链接/有序列表渲染

# P2 — 中期迭代（2-4 周）：
#   [1.4] 季节化快捷问题
#   [2.3] 分享功能
#   [4.1] 接入真实天气 API
#   [4.2] 会话迁移到 Redis
#   [5.1] 增加更多登录方式 / 游客模式

# P3 — 长期规划：
#   [2.4] 上下文提示
#   [3.2] 消息操作菜单
#   [3.3] 输入体验增强（多行/语音）
#   [4.3] 代码重复消除
#   [5.2] 内容安全审核
